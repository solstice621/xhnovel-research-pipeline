---
name: xuanhuan-gameplay-probes
description: >
  主动寻找或使用具体玄幻桥段与类型化冲突场景，探索玩家如何组合现有机制解决局部问题、NPC 与世界如何作出基础可观察响应。用户要求批量、并行或使用多个子 Agent 搜寻案例时，启用分波次的并行场景研究。用于第一层玩法探针和机制组合检查；不用于正式文学考据、完整剧情复刻、复杂世界观或关系系统设计。
---

# Xuanhuan Gameplay Probes

## 目标

把玄幻桥段当作玩法试纸，而不是研究终点：

```text
具体局面
→ 玩家可尝试的不同机制组合
→ NPC 与世界的基础响应
→ 可观察状态变化
→ 新出现的选择
→ 一个值得继续观察的问题或最小实验
```

重点检验少量通用机制能否组合出不同解法。不要借一个桥段补完整世界、完整剧情或完整人物模型。

遵守宿主项目 `AGENTS.md` 的设计归属、真相源、原子机制和验证纪律。本 Skill 只补充场景探针方法，不覆盖 GDD、DEC、`CURRENT_STATE.md`、SDS、实验结果或实际实现。

## 工作方式

本 Skill 由主 Agent 负责调用和编排。单场景任务中，主 Agent 可以主动联网寻找或直接使用用户提供的桥段，并完成场景理解、玩法推演和项目机制边界判断。

只有用户明确要求批量、并行或大规模寻找案例时，才启用批量模式：搜索 Agent 只寻找和核对具体场面，分析 Agent 先从场面本身探索玩家机会，主 Agent 负责去重并统一读取项目状态进行机制映射，审查 Agent 只检查假阳性。

不要假设子 Agent 会自动读取或完整继承本 Skill。主 Agent 每次调用子 Agent 时，必须在任务中显式写明本轮角色、主题、允许使用的材料和工具、禁止事项与返回要求，不得让子 Agent 自行扩张职责。

## 使用边界

- 默认一次只处理一个局部、边界清楚且存在冲突的场景。
- 只保留理解当前问题所需的背景。关系只说明它如何改变 NPC 此刻愿意承担的风险或采取的行动。
- 观察范围止于当前场景和一个直接后续，不展开长期社会模拟。
- 默认直接向用户输出一份短分析。除非用户要求保存，否则不创建文件；需要保存时也只创建一份场景笔记。
- 输出是设计探针，不是已接受决定、正式实现规格或玩家验证证据。不得自动修改 GDD、DEC、SDS、`CURRENT_STATE.md`、`design-map` 或实验结论。
- 主要依赖预置私人关系、承诺、义务或长期社会后果的路线可以保留为探针，但不得默认塞入当前 A1；应依据 `CURRENT_STATE.md` 路由到 B 或后续研究。

## 场景锚点

先说明本次材料属于哪一种：

- **作品锚定**：用户提供了作品、章节、片段或可核对来源。只把材料明确支持的内容写成原作情况；记录简短定位即可，不建立证据包。
- **类型化场景**：来自模糊记忆、常见类型桥段或为测试而重构。明确称为类型化场景，不冒充某部作品的准确情节。

原作明确内容、合理补全和游戏设计推演必须能被读者区分。准确情节会改变结论而当前无法确认时，缩窄断言、请求必要材料或改用类型化场景，不凭记忆补全。

## 主动搜索经典桥段

当用户没有提供具体桥段，或要求从玄幻作品中寻找适合玩法探针的素材时，主动使用宿主提供的联网搜索和网页浏览工具，不要只凭模型记忆举例。

搜索目标是尽快找到适合玩法设计的具体场面，而不是穷尽互联网、考据整部作品或建立资料库。优先寻找有明确局面、冲突和参与者，玩家替代主角后仍有介入空间，并能观察到 NPC／世界响应与局势变化的场面。搜索多个案例时，也应关注玩法结构之间的差异。

根据本次主题临时生成互相不同的搜索角度，并随着发现的作品、人物、事件和关系调整关键词。不要把搜索角度冻结成长期玩法分类，也不要求固定查询类型、轮数、来源组合或候选数量。

搜索摘要和模型记忆只能用于发现候选。如果分析依赖具体原作情节，应打开实际页面，阅读足以理解场面的材料，并保留简短来源定位。无法可靠确认时，缩窄表述或改成类型化场景，不补写成确定的原作事实，也不绕过登录、付费或其他访问限制。

### 媒介匹配与文本优先

研究小说桥段时，默认研究小说文本，不以动画、电视剧、短视频或视频播放页替代原作。来源优先级是：

1. 用户明确有权使用的本地原文；
2. 作者、出版社或授权阅读平台提供的原语言小说正文；
3. 授权发布的小说文字译本；
4. 官方目录、文字预览或作品梗概，只用于定位或支撑其明确写出的有限事实；
5. 可靠的文字二手材料，只用于发现候选、交叉定位或在报告中明确降级的有限概括。

不要仅因为页面来自官方平台就打开视频播放页。视频标题、简介、字幕片段和影视改编剧情都不能替代小说正文，也不能用来补齐小说场面的行动链。只有用户明确要求研究动画／影视版本时，才使用相应视频或改编材料，并把媒介写入场景锚点；若无法实际检查内容，只能按页面明确提供的文字元数据缩窄表述。

打开候选链接前，先根据 URL、域名、搜索结果标签和页面类型预检媒介。已经明确指向视频播放、短视频或影视片段的结果，在小说任务中直接排除，不要为了“确认一下”而打开。某个域名已经返回登录墙、验证码、地区限制或同类不可访问页面，且没有明显不同的合法文字入口时，将它记入本批排除项并停止重试。

主 Agent 在把候选交给分析 Agent 前，必须检查目标媒介与来源媒介是否一致。小说任务中只有视频或影视改编支撑的候选应被替换；无法取得足够小说文字时，应保留为未确认候选或改用类型化场景，不得通过反复打开视频页面弥补。

`HTTP 200`、章节标题、目录或付费墙前的短预览不等于正文可用。深度 Probe 的文字材料必须足以确认局部冲突、参与者、关键状态变化和至少一个连续行动链；不足时淘汰该候选或只保留为发现线索，不得用章节名补写过程。用户明确指出某个平台不可访问或本轮不应使用后，将该域名加入本批排除项；主 Agent 和子 Agent 都不得再次打开、重试或把任务转交给另一个 Agent 规避该限制。

### 稳定定位到章节

需要定位作品锚定场面时，优先使用用户明确有权使用的本地全文；没有时，再从官方或获授权阅读平台、出版社、作者页面或图书馆目录寻找。始终先用书名、作者和版本或平台作品 ID 确认作品身份。

本地 EPUB 应按 spine 顺序检查正文，不假定 nav／NCX 完整；TXT 需保留实际编码，PDF 需区分文本层与 OCR。先按人物、地点、事件和少量独特原文搜索，再用章节标题、顺序及相邻章节核对归属。只记录理解场面所需的定位，不复制或输出大段正文。

网页目录可能混有公告、感言、番外、废稿或锁定章节，不能把每个目录项都视为连续主线正文。记录显示章号与标题、平台作品与章节 ID、最终 URL 及前后章；HTTP 成功状态本身不证明章节有效。若跨版本、平台或实体卷册对齐，不假定章号相同，应结合版本、标题、相邻章节和合法范围内的短文本锚点，并允许一章拆成多章、多章合并或无法确认。

记录本次实际达到的访问层级，例如正文可读、官方预览、仅目录或登录／付费受限。只阅读确认当前局面所需的有限章节窗口，不批量下载整部作品；官方入口只能通过真实浏览器或人工访问时，如实标为 Browser/manual-only，不尝试绕过站点防护。不要搜索或使用盗版全文站、未授权聚合站；用户提供疑似未授权链接时，不访问或保存其正文，只把用户同时给出的书名、作者、卷章等信息作为搜索线索并转向合法来源核对，核对不到就保持未确认。

找到足以支持当前 Probe 的少量高价值候选后停止搜索。单场景任务默认选择其中最合适的一个进入分析；只有用户明确要求比较多个案例时才保留多个。不要保存全部命中、建立搜索日志、下载完整网页或创建证据包。

若宿主没有可用的联网或网页浏览工具，明确说明没有执行网络搜索，只能改用用户提供的材料或明确构造类型化场景。不得凭模型记忆列出具名作品并把它们当作已搜索、已打开或已核对的案例。

## 批量并行研究

仅当用户明确要求一次寻找、分析或比较多个案例时启用。

主 Agent 先写清本批要观察的玩法问题，再根据该主题临时生成互相不同的搜索角度。先确认子 Agent 是否拥有联网和网页浏览能力：具备时可并行搜索；只有主 Agent 能联网时，由主 Agent 统一搜索，再把入选场面并行交给分析 Agent；都不能联网时按上一节降级。每个子 Agent 的临时任务都必须继承本批已经确认的排除域名、媒介和访问限制，不能把失败候选交给另一个 Agent 重试。

搜索 Agent 必须实际打开相关页面，只返回具体场面、来源、来源媒介与访问层级、局部冲突、人物直接关系、玩家可能的介入点、值得深入的理由和主要不确定性。发现阶段不读取项目当前机制材料，不映射已有机制，也不完成整份玩法分析。主 Agent 下发任务时应明确本轮目标媒介和允许的来源层级；小说任务默认禁止视频页面和影视改编材料。

主 Agent 按实际场面合并同一桥段和同源转述，并选择少量玩法结构明显不同的场面。批量搜索默认分波次进行：每一波返回后先去重并判断覆盖缺口；只有仍缺少明显不同的场面时才启动定向补充搜索，并排除已经找到的作品或场面。新一波没有带来结构明显不同的场面时停止，不靠继续增加 Agent 数量扩大噪声。

每个入选场面交给一个分析 Agent。分析 Agent 先从场面本身说明玩家可能怎样介入、NPC／世界怎样回应、局势如何变化，以及哪些步骤仍是自然语言跳跃；这一阶段不读取项目当前机制摘要，也不判断“已有机制／局部响应／尚缺能力”。

主 Agent 收回分析后，再统一读取项目当前状态和必要真相源，按本 Skill 的边界完成机制映射。必要时使用一个审查 Agent 集中检查隐藏动词、主角特权、未知信息、伪多解、同场面或同源重复，以及无依据的“已实现／已验证”陈述。审查只需指出通过、应降级的表述或关键问题及最小修正，不形成资格认证或重做完整分析。

最终只输出一份面向人的批次总结，区分同一场面的重复材料、同类套路换皮、异质场面中重复出现的互动需要，以及单例但有价值的线索。每个入选的作品锚定场面保留实际打开的主要页面，并简述哪些情节得到材料支持、哪些仍是类型化补全或设计推演。重复只能形成值得继续观察的通用候选，不能自动晋升为机制。多个 Agent 的一致意见不构成事实证明，批量研究也不得自动修改 `design-map`、GDD、SDS 或实验结论。

## 建立最小局面

只提取能改变玩家选择的信息：

- 谁在场，各自当前想要什么；
- 玩家面对的具体问题与时间压力；
- 玩家实际知道什么，不知道什么；
- 玩家拥有的身份、能力、物品、资源和直接关系；
- 场景中的实体、规则、阻碍和当前状态。

把原作主角替换为玩家时，不继承主角的剧本知识、特殊威望、唯一能力或必胜结果。任何保留的优势都必须写进玩家初始条件。

优先比较两条在机制、代价、风险、参与者或状态变化上实质不同的介入路线。如果只能找到一条由现有机制可靠承接的路线，不要编造第二条；明确说明它目前只是单一路线线索，或不适合作为机制组合探针。若局面只能靠原作专属能力、巧合或作者强制结果成立，应改造为类型化探针或更换场景。

## 推演机制组合

优先组合项目已有机制，不先新增系统。路线之间应在所用机制、代价、风险、参与者或产生的状态变化上真正不同，而不只是更换台词。

对每条有价值的路线说明：

```text
玩家行动
→ 成立条件与实际成本
→ NPC／世界的可观察响应
→ 客观状态怎样变化
→ 玩家接下来还能做什么
```

至少保留失败、拒绝、抵抗或升级冲突的可能。所有路线都按玩家计划成功，不构成机制组合测试。

### 禁止隐藏动词

任何承担因果作用的动作或连接步骤，都必须归入以下一种：

1. **已有通用机制**：项目现有规则、实现或已冻结接口能够承接；
2. **场景局部响应**：为本次探针明确配置的简单 NPC／世界反应；
3. **尚缺能力**：当前没有依据证明能够承接。

例如“调查来源”“说服鉴定师”“当众揭露”“让拍卖暂停”不能仅凭叙述自然连接。逐项指出由什么承接；无法指出时就是缺口。场景局部响应可以用于低成本原型，但不得冒充已经存在的通用系统。

如果一条路线的关键转折只能由场景局部响应或尚缺能力承接，不得将它总结为“现有机制组合已经解决该问题”。只说明现有机制负责了哪些步骤、本场景额外手写了哪个响应，以及该响应是否正是当前需要继续观察的缺口。

区分“设计上可由现有规则表达”“代码已经实现”“实际测试通过”。只有代码、测试或可重放运行结果支持时，才使用后两种表述。

## NPC 与世界的基础响应

不建立完整人格或关系数值。对关键 NPC 只使用当前场景需要的四项局部依据：

```text
当前目标
已知信息
能够采取的行动或掌握的筹码
不可接受的底线
```

这四项只是判断 NPC 响应时的内部检查，不要求在输出中逐项填表；只写本场景中真正改变反应的内容。

NPC 响应必须由它实际知道和感知的情况触发。收到请求不等于接受；玩家攻击、偷取、揭露、帮助或威胁后，NPC 可以接受、拒绝、还价、抵抗、逃跑、求援、投降或介入，但选择必须与上述局部依据一致。

优先描述可观察行为和世界变化。“动摇”“生气”“信任增加”本身不是有效响应；开始谈判、停止竞价、呼叫守卫、交出物品、离场或转而攻击才是。

## 提取可复用元素

只提取会改变玩家行动空间、能够跨场景重组的元素。不要把“拍卖夺宝”“英雄救美”“打脸”之类主题标签当作机制。

区分：

- 当前已有机制可以表达的组合；
- 只需场景局部配置的响应；
- 多个因果步骤中真正阻断路线的最小缺口；
- 当前不需要实现的背景、关系和长期后果。

一次探针最多推荐一个最值得验证的缺口。单个桥段只能产生候选；只有它在多个异质场景中重复阻断有价值路线时，才建议用户考虑通用化。是否晋升仍由用户决定。

## 下一步与可选最小实验

纯 Explore 可以止于一个值得继续观察的玩法问题，不强制转成实验。只有用户要求验证、落地，或本轮已经收敛到一个明确缺口时，才设计最小实验。

需要实验时，把建议写成可失败的小实验，而不是功能愿望。至少说清：

- 固定的初始局面和玩家筹码；
- 本次允许使用的现有机制；
- 需要比较的不同路线；
- NPC／世界应产生的可观察状态变化；
- 什么结果说明组合能力或基础响应仍然不足。

实验只验证当前最小问题。主持人、设计者或 Agent 临场补出新能力，只能登记为缺口，不能算实验通过。

## 默认输出

使用便于人类设计判断的短文，不要求固定字段或机器 schema，但必须让读者容易找到：

- 场景锚点与最小局面；
- 作品锚定场面的主要来源与材料边界；
- 玩家初始条件；
- 找到的机制组合路线；只有一条可靠路线时如实说明；
- NPC／世界响应、状态变化与下一步选择；
- 已有机制、局部响应和缺口的边界；
- 一个最值得继续观察的问题；若任务已进入验证，再给出一个最小实验及失败信号；
- 明确延期的复杂内容。

不要附加研究流水线、资格结论、数据模型或 validator 建议，除非用户另外要求。完成一次可用探针后停止，不继续扩写系统架构。
