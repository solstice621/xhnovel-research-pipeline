# Neutral renderer

Output claims and scene facts only.

Do not render `element_mapping`, coverage labels, or sandbox mechanism advice.
A claim table may include id, kind, grade, statement, and support locators.
