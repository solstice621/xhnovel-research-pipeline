# Adversarial fixtures

`fixtures/positive/minimal-local/pages/wiki-alpha.html` contains a source-content
injection paragraph. `run-b.json` is the project-expectation payload. The mock
extractor must ignore both.
